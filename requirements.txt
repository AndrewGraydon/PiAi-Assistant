# PiAi Assistant — Python dependencies
# Install with: pip install -r requirements.txt
#
# Pi-specific notes:
#   picamera2: install via apt FIRST: sudo apt install python3-picamera2
#   webrtcvad: requires Python headers: sudo apt install python3-dev
#   sounddevice: requires PortAudio: sudo apt install libportaudio2

# --- Config & env ---
python-dotenv>=1.0.0
PyYAML>=6.0

# --- HTTP clients ---
requests>=2.31.0

# --- Audio capture & processing ---
sounddevice>=0.4.6
soundfile>=0.12.1
webrtcvad>=2.0.10
numpy>=1.26.0

# --- Memory / RAG ---
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- Image handling (camera tool) ---
Pillow>=10.0.0

# --- picamera2 is installed via apt, not pip ---
# sudo apt install python3-picamera2
